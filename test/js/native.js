/* TESTING NATIVE */

function myfunc(){
        var x = void(0);
        if(true){}else if(false){}else{}
        switch (x) {
        case '_nextValid': // "_nextValid" is a reserved index within the JSON file
                myfunc(null);
                break;
        case '_ignore': // "_ignore" is a reserved index within the JSON file
                myfunc(null);
                break;
        default:
                break;
        }
        return {
                hello:"world"
        };
}
do{myfunc();}while(x < 2);
do
{myfunc();}while(x < 2);
do {myfunc();}while(x < 2);
document.getElementById('something').innerHTML;
var la;
if (typeof this === "string"){}
if (la instanceof "String"){}
if (la instanceof"String"){}
throw new Error();
delete la;
try {
        something();
} catch(e) {
        yesFail();
} finally {
        whoFoundUseForThis('?');
}
with(this){
        douglasNotHappy();
}
debugger;
for (var carefuljavascriptdoesnotsupportblockscope=0;carefuljavascriptdoesnotsupportblockscope<10;carefuljavascriptdoesnotsupportblockscope++){___okey___--;}
